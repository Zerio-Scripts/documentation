# Installation Guide

This does not require any specific installation steps, other than looking through the configuration file and making sure everything is set up the way you want to.

The SQL files do get executed automatically, although you have the possibility to execute them manually via the `zerio-cardealer/sql/*.sql` file(s).
